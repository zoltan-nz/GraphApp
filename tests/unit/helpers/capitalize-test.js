import {
  capitalize
} from 'graph-app/helpers/capitalize';

module('CapitalizeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = capitalize(42);
  ok(result);
});
